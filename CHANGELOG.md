# 更新日志

本项目采用[语义化版本](https://semver.org/lang/zh-CN/)：修复发布为补丁版本，兼容功能发布为次版本，不兼容变更发布为主版本。已经发布的 Git 标签永久保留，不移动、不覆盖。

## [1.0.5] - 2026-09-08

修复班级卡片显示已有学生，但管理员打开注册学员名单等待后误显示“该班级暂无学生”的问题。

- 班级名单接口不再返回完整 base64 头像和无关资料，只返回展示、复制和导出所需字段。
- 已上传头像改用现有独立头像接口按需加载，名单数量与班级卡片继续读取同一 MySQL 数据。
- 请求失败、超时或管理员登录失效时显示真实错误和“重新加载”，不再伪装为空班级。
- 新增 46 人轻量名单单元测试和真实 MySQL 查询测试并纳入 CI。
- 模拟 46 人全部带头像时名单 JSON 为 13,600 字节且不含 base64。
- 无数据库结构变更，不需要迁移，现有学生、账号、密码和照片保持不变。

### 生产验证记录（2026-09-09）

- 生产服务器固定升级至 `v1.0.5` 成功，发布脚本确认升级前后 MySQL 的 11 项数据清单一致。
- 线上保留 7 个班级、89 个账号、88 名学生、9 个同义词组、116 个标准爱好和 50 份已存头像；孤立账号为 0。
- 本机与公网健康、版本和照片墙接口均通过；公网照片墙返回 HTTP 200、37,509 字节，耗时约 0.016 秒。
- 服务端巡检全部通过但浏览器受保护页面仍不可用时，应先退出管理员账号并重新登录，必要时清理该站点缓存或使用无痕窗口；不要重复部署、重建数据库或恢复备份。
- 完成浏览器会话刷新后，用户确认网页和学生名单恢复正常。

## [1.0.4] - 2026-09-08

把 v1.0.2 → v1.0.3 生产升级、照片墙排障、MySQL 数据保护和 gzip 冲突恢复过程整理为可复用流程。

- 新增 `check-production.sh`，一条命令只读检查三个服务、Nginx、运行版本、MySQL 清单、本机与公网照片墙响应。
- 发布脚本自动安装 `/usr/local/sbin/check-personalink`，方便后续巡检和故障反馈。
- CI 自动检查备份、发布和巡检三个 Ubuntu 脚本的 shell 语法。
- Ubuntu 教程新增真实现场案例，明确 HTTP 可正常使用而 HTTPS 可暂缓配置。
- 软件说明与 CI/CD 文档统一给出一行一条、带注释且可直接复制的命令。
- 无数据库结构或业务接口变更，不需要迁移现有数据。

## [1.0.3] - 2026-09-08

修复 Ubuntu 默认 Nginx 已启用 gzip 时，发布脚本再次声明 `gzip on` 导致 `nginx -t` 报 duplicate 并在 `[8/9]` 停止的问题。

- 保留 `deploy/personalink-gzip.conf` 为无指令兼容占位文件，使已安装的 v1.0.2 发布脚本也能平滑部署 v1.0.3。
- gzip 继续由站点 `nginx.conf` 配置，不影响照片墙轻量接口和静态资源压缩。
- GitHub Actions 官方组件升级至 Node.js 24 运行时版本，消除 Node.js 20 弃用提示。
- 补充现场故障判断、可恢复停用方法和数据核验记录。
- 无数据库结构变更，不运行迁移；生产现场升级前后 11 项清单一致，7 个班级、88 名学生和 50 份已存头像全部保留。

## [1.0.2] - 2026-09-08

新增带数据保护的统一发布脚本和 GitHub Actions CI/CD，并把部署命令整理为带中文注释、每行可直接执行的形式。

### 自动检查

- CI 自动核对 7 处版本号和全部后端 JavaScript 语法。
- CI 使用临时 MySQL 完成建表、模拟数据检查、导入和数据库完整性验证。
- CI 使用 lockfile 安装依赖、构建前端并保留 14 天构建产物。

### 安全部署

- `deploy-release.sh` 只接受语义化版本标签，拒绝脏工作区和不存在的版本。
- 部署前生成 MySQL 四件套备份，部署前后比对 11 项数据库清单。
- CD 只允许手动部署已发布的 GitHub Release，并支持 production Environment 审核。
- 同一时间只运行一个生产发布任务，不取消正在执行的备份或部署。

### 文档

- 新增 CI/CD、SSH 最小权限、Secrets、故障处理和安全边界说明。
- Ubuntu 部署教程优先推荐固定标签和统一发布脚本，保留 `main` 手工更新作为开发测试方案。

## [1.0.1] - 2026-09-08

修复低带宽或网络抖动时照片墙频繁显示“网络错误”的问题；数据库结构和数据不变。

### 修复与优化

- 照片墙列表不再内嵌体积较大的 base64 头像，改为轻量列表加按需头像接口。
- 班级列表与照片墙并行加载，缩短首次等待时间。
- GET 请求遇到瞬时连接失败时自动重试一次，请求超时由 10 秒调整为 30 秒。
- 根据超时、服务器不可达和设备离线状态显示更准确的错误提示。
- Nginx 模板启用 JSON、JavaScript 和 CSS gzip 压缩。

### 线上问题证据

- 2026-09-08 实测旧版 `/api/photowall` 响应约 1.54 MB，连续三次在 60 秒内未传输完成。
- 同时观察到少量 TCP 建连超时；旧版前端固定 10 秒超时，因此容易误判后端未启动。
- HTTP `/health` 正常且 MySQL 可用；HTTPS 443 当时未开放，属于服务器 HTTPS 配置问题。

### 升级说明

- 无数据库结构变更，不需要重新建表或导入数据。
- 更新代码后执行后端 `npm ci --omit=dev`、前端 `npm ci && npm run build`，再重启后端服务。
- 线上启用 gzip 时，应把新版 Nginx 配置项合并到现有域名配置并执行 `sudo nginx -t`。

## [1.0.0] - 2026-09-04

首个可部署、可备份、可恢复的稳定版本。

### 功能

- Vue 3 前端：注册登录、个人资料、班级、照片墙、搜索、同义词和管理员页面。
- Express 后端与 MySQL 持久化，密码使用 bcrypt 哈希。
- 管理员可维护班级和用户、重置普通用户密码、导出班级名单及修改自己的密码。
- 内置经过核验的教学模拟快照，可导入 3 个班级、42 名学生和 1 名管理员。
- 前端显示版本号，后端提供 `/api/version`，健康检查返回版本号。

### 部署与数据

- 提供 Ubuntu 24.04、Nginx、systemd、HTTPS 的完整部署教程。
- 提供 MySQL 建表、模拟数据导入、数据核验、备份、校验、恢复及换服务器流程。
- 备份包含 SQL gzip、SHA-256、Git commit 和数据库清单四件套。
- 固定演示数据库应用账号为 `personalink`，密码为 `123456`，便于教学部署。

### 验证

- 已验证前端生产构建、后端语法、MySQL 建表与迁移、核心接口和管理页面。
- 已验证数据库备份及新库恢复，恢复后班级、用户、同义词和头像数据一致。

### 已知边界

- `backend/db.json` 仅为公开教学模拟数据；网站运行后的实时数据必须通过 MySQL 备份迁移。
- 普通用户接口尚未全面实现 Session/JWT 权限隔离；承载真实公网用户前需进一步加固。
- `123456` 仅用于当前教学演示配置；接入真实数据时应改为独立强密码。

[1.0.5]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.5
[1.0.4]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.4
[1.0.3]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.3
[1.0.2]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.2
[1.0.1]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.1
[1.0.0]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.0
