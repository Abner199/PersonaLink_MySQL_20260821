# 更新日志

本项目采用[语义化版本](https://semver.org/lang/zh-CN/)：修复发布为补丁版本，兼容功能发布为次版本，不兼容变更发布为主版本。已经发布的 Git 标签永久保留，不移动、不覆盖。

## [1.0.0] - 2026-09-04

首个可部署、可备份、可恢复的稳定版本。

### 功能

- Vue 3 前端：注册登录、个人资料、班级、照片墙、搜索、同义词和管理员页面。
- Express 后端与 MySQL 持久化，密码使用 bcrypt 哈希。
- 管理员可维护班级和用户、重置普通用户密码、导出班级名单及修改自己的密码。
- 内置经过核验的教学模拟快照，可导入 3 个班级、42 名学生和 1 名管理员。
- 前端显示版本号，后端提供 `/api/version`，健康检查返回版本号。

### 部署与数据

- 提供 Ubuntu 24.04、Nginx、systemd、HTTPS 的完整部署教程。
- 提供 MySQL 建表、模拟数据导入、数据核验、备份、校验、恢复及换服务器流程。
- 备份包含 SQL gzip、SHA-256、Git commit 和数据库清单四件套。
- 固定演示数据库应用账号为 `personalink`，密码为 `123456`，便于教学部署。

### 验证

- 已验证前端生产构建、后端语法、MySQL 建表与迁移、核心接口和管理页面。
- 已验证数据库备份及新库恢复，恢复后班级、用户、同义词和头像数据一致。

### 已知边界

- `backend/db.json` 仅为公开教学模拟数据；网站运行后的实时数据必须通过 MySQL 备份迁移。
- 普通用户接口尚未全面实现 Session/JWT 权限隔离；承载真实公网用户前需进一步加固。
- `123456` 仅用于当前教学演示配置；接入真实数据时应改为独立强密码。

[1.0.0]: https://github.com/Abner199/PersonaLink_MySQL_20260821/releases/tag/v1.0.0
