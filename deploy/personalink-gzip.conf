# PersonaLink 文本响应压缩；安装到 /etc/nginx/conf.d/personalink-gzip.conf。
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_comp_level 5;
gzip_types application/json application/javascript text/css text/plain image/svg+xml;
